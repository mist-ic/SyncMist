# https://moonrepo.dev/docs/config/toolchain
$schema: 'https://moonrepo.dev/schemas/toolchain.json'

# Node.js for any JS tooling (using Bun as package manager)
node:
  version: '20.10.0'
  packageManager: bun

# Bun configuration
bun:
  version: '1.1.0'
